{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "clean": "node -e \"const fs=require('fs');['.next','node_modules/.cache'].forEach(p=>{try{fs.rmSync(p,{recursive:true,force:true,maxRetries:5,retryDelay:100});console.log('removed',p)}catch(e){console.warn('could not remove',p,e.message)}})\"",
    "dev:clean": "npm run clean && next dev"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.103.0",
    "framer-motion": "^12.38.0",
    "jose": "^6.2.2",
    "next": "14.2.35",
    "razorpay": "^2.9.6",
    "react": "^18",
    "react-dom": "^18",
    "sass": "^1.99.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "typescript": "^5"
  }
}
